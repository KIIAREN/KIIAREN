/**
 * Create a new workspace.
 *
 * @deprecated Use `useCreateWorkspace` from `@/lib/backend` instead.
 * This is a re-export for backward compatibility.
 *
 * @example
 * ```tsx
 * import { useCreateWorkspace } from '@/lib/backend';
 * // or
 * import { useCreateWorkspace } from '@/features/workspaces/api/use-create-workspace';
 * ```
 */
export { useCreateWorkspace } from '@/lib/backend';
